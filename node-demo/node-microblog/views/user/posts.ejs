<% posts.forEach(function(post,index){ %>
    <% if(index % 3 == 0){ %>
        <div class="row">
    <% } %>
    <div class="col-md-3">
        <h2><a href="/u/<%= post.user %>"><%= post.user %></a> 说</h2>
        <p><%= post.post %></p>
    </div>
    <% if(index % 3 == 2){ %>
        </div>
    <% } %>
<% }) %>
<% if(posts.length % 3 != 0){ %>
    </div>
<% } %>